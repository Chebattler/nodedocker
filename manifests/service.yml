apiVersion: v1
kind: Service
metadata:
    name: "t5-1c0e"
    labels:
        app: "t5-1c0e"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "t5-1c0e"